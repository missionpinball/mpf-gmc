# Normalize line endings for all files that Git considers text files.
* text=auto eol=lf

# Only include the addons folder when archiving the repository,
# which makes the zip file downloaded from the Asset Library.
/**        export-ignore
/addons    !export-ignore
/addons/** !export-ignore